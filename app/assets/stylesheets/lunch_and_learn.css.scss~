// Place all the styles related to the lunch_and_learn controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

h1 {
    small {
	font-size:medium;
	  }
    }
div {
    .inline {
         display: inline;
         color:  #ffff;
	   }
     }
